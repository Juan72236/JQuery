//$(document).ready(function(){
    //agregar efectos asi:
    // $('.caja').hide();
    function efecto(){
        //$(selector).efecto(velocidad, callback);
        //$('.caja').hide(3000, function(){
        //alert('oculto');
        //esta es una funcion que se ejecuta luego de que el efecto se haya ejecutado


        // $('.caja').toggle(3000); me permite aparecer y desaparecer el elemento al hacer click en el boton.
        // $('.caja').fadeIn(1000); muestra un elemento oculto pero con un efecto
        // $('.caja').fadeOut(1000); oculta un elemento con un efecto
        // $('.caja').fadeToggle(); permite ocultar y mostrar el elemento al tiempo, oprimiendo un boton.

        // $('.caja').slideDown(); otra animacion para aparecer objetos
        // $('.caja').slideUp(); otra animacion para ocultar objetos
        // $('.caja').slideToggle(); alterna los dos anteriores
    }

    
//});